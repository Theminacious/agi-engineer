# Sample Run: run-2026-01-12-sample

## Overview

This directory contains a frozen, immutable snapshot of one complete AGI Engineer run. It represents a real execution with all governance steps, approvals, fixes, and sealing.

**Purpose:** Enable third-party verification without trusting AI output or reading source code.

---

## What This Run Represents

**Repository:** A sample Python project with ~500 lines of code across 8 files.

**Issues detected:** 18 total issues (unused imports, line-too-long violations, trailing whitespace, excessive blank lines).

**Fixes applied:** 5 fixes (all high-confidence unused import removals and one line-length fix).

**Duration:** 5 minutes 30 seconds (wall-clock time).

**Final state:** COMPLETE (run finished successfully).

---

## Why This Run Is Representative

This run demonstrates:

1. **Complete governance lifecycle:**
   - Policy resolution
   - Human approval required and recorded
   - Safety checks performed
   - Fixes applied in sequence
   - Test validation
   - EDR generated
   - Terminal sealing

2. **Typical fix patterns:**
   - Unused imports (F401) — common, high-confidence, low-risk
   - Line-too-long (E501) — common, medium-confidence, low-risk
   - Low-confidence issues rejected (not attempted)

3. **Human-in-the-loop:**
   - Approval by `alice@company.com` at timestamp `2026-01-12T10:01:30Z`
   - Explicit actor recorded in immutable event

4. **Deterministic execution:**
   - Event sequence is contiguous (0, 1, 2, ..., 17)
   - No invariant violations
   - Replay produces identical state

---

## Artifacts in This Directory

### 1. ledger.json
Run metadata: timestamps, policy, final state.

### 2. events.jsonl
Complete event stream. 18 events from RUN_CREATED to RUN_COMPLETED.

### 3. inspect_run.json
Comprehensive inspection report generated by `run_inspect.py` (frozen, do not regenerate).

### 4. replay_summary.json
Deterministic replay output from `run_replay.py` (frozen, do not regenerate).

### 5. README.md (this file)
Human-readable context and verification guide.

---

## How to Use These Artifacts

### Quick Verification (2 minutes)

1. **Inspect the run:**
   ```bash
   python3 agent/run_inspect.py examples/sample_run
   ```
   Expected: JSON output with `final_state: "COMPLETE"` and `invariants.violation_count: 0`.

2. **Replay the run:**
   ```bash
   python3 agent/run_replay.py examples/sample_run
   ```
   Expected: `invariants: 0`, `fixes: 5`, `plan_approved: True`.

3. **Verify sequences:**
   ```bash
   python3 -c "
   import json
   with open('examples/sample_run/events.jsonl', 'r') as f:
       events = [json.loads(line) for line in f if line.strip()]
   sequences = [e['sequence'] for e in events]
   expected = list(range(len(events)))
   print('Valid:', sequences == expected)
   "
   ```
   Expected: `Valid: True`

### Deep Audit (10 minutes)

Follow the complete verification checklist in [docs/PROOF_OF_TRUST.md](../../docs/PROOF_OF_TRUST.md).

Verify:
- [ ] Event sequences are contiguous
- [ ] Human approval is recorded
- [ ] Fixes occurred after approval
- [ ] Safety checks passed before fixes
- [ ] Terminal state matches ledger
- [ ] No invariant violations

---

## What This Proves

By verifying this sample run, you prove:

1. **Immutability:** The ledger cannot be retroactively edited.
2. **Determinism:** Replay produces the same state every time.
3. **Governance:** Human approval and safety checks are recorded.
4. **Causality:** Fixes are traceable to approvals and safety validations.
5. **Completeness:** The run has a terminal state and no sequence gaps.

---

## What This Does NOT Prove

- **Correctness:** We don't claim the fixes are objectively correct (context-dependent).
- **General intelligence:** The system is not "smart"; it follows governance rules.
- **Robustness:** This proves one specific run, not all possible runs.

---

## Confidentiality Note

This sample run contains:
- **No secrets** (no API keys, passwords, tokens)
- **No environment-specific paths** (all paths are generic or relative)
- **No proprietary code** (the repository is a synthetic sample)

It is safe to share publicly.

---

## For Compliance Teams

This artifact set satisfies:
- **Audit trail requirement:** Every decision is recorded.
- **Deterministic replay requirement:** Run can be reproduced.
- **Authorization requirement:** Human approval is recorded.
- **Integrity requirement:** Sequences are contiguous and validated.

Compliance verification steps are in [docs/PROOF_OF_TRUST.md](../../docs/PROOF_OF_TRUST.md).

---

## Questions?

If you encounter issues verifying this run:

1. Ensure Python 3.8+ is installed.
2. Ensure you are in the repository root directory.
3. Check that `agent/` modules are accessible (no import errors).
4. Run verification commands exactly as documented.

If invariant violations are detected, this indicates either:
- A bug in the replay engine (report to maintainers).
- Artifact corruption (re-download from source).

---

## Summary

This frozen sample run is proof that AGI Engineer:
- Records decisions immutably.
- Enforces governance deterministically.
- Enables third-party verification without trust.

This is not marketing. This is evidence.
